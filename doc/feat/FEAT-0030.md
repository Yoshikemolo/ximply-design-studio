---
id: "FEAT-0030"
title: "FEAT-0030 — Convert to pixel image"
status: "implementing"
domain: "feat"
owners: ["Yoshikemolo"]
applies_to: ["ximply-design-studio"]
related: ["SC-0123", "SC-0124", "FEAT-0007", "FEAT-0010", "FEAT-0029", "ADR-0036", "SEC-0005", "PO-0001", "PLAN-0030-0001", "SC-0149"]
source: ["Owner request of 2026-09-23 for AI Tools and Change control"]
---

# FEAT-0030 — Convert to pixel image

Status: Implementing
Owner: Yoshikemolo
Iteration: ITER-0004
Dependencies: FEAT-0007, FEAT-0010
Decisions: none new; the result is an ordinary image layer of the current native format
Security controls: SEC-0005
Plan: [PLAN-0030-0001](../implementation/PLAN-0030-0001.md)

## Outcome

The designer converts the selected objects, or every visible layer of the document when
nothing is selected, into a PNG with transparency that is inserted as a new image object
above the selection. The source objects stay unchanged. The AI Tools of FEAT-0029 use
the same conversion to attach a preview of the canvas and of the selection to the
context of a request, so the model sees the same pixels the designer would get.

## Functional scope

- A sub-tool of the current Import image tool, named Convert to pixel image, reachable
  from the flyout of the image button in the tool rail and from the Import entry of the
  toolbar. For Illustrator parity the plan proposes listing the same command as
  Object > Rasterize…; that entry waits for the owner's confirmation.
- A dialog with the options that affect the result: resolution (72, 150 and 300 ppi and
  another value up to 2400 ppi), anti-aliasing (art optimized or none) and space added
  around the objects, in the distance unit. The background is always transparent. The
  picture is cropped to its painted pixels on a pixel grid anchored at the page origin.
- With a selection, the image covers the visible bounds of the selected objects,
  strokes and effects included, and is inserted directly above the topmost selected
  object, but outside every group: just above the outermost group that holds that object
  (owner request of 2026-09-24, SC-0149). With no selection, it covers every visible,
  printing layer of the document and is inserted at the top of the layer stack. Hidden
  layers and nonprinting guide layers (ADR-0024) are not drawn.
- The new image layer is selected, is named from the localized catalog and records one
  undo step. It is a placed picture, not a paint layer (ADR-0036), so its frame never
  changes by painting.
- The pixels are drawn by the same renderer as PNG export, so the result matches an
  export of the same area at the same resolution.
- A result without visible pixels, or larger than 8192 pixels a side or 32 megapixels,
  is refused whole with a message and leaves the document unchanged.
- A programmatic entry point returns the PNG without inserting it; FEAT-0029 uses it for
  its canvas and selection previews, capped at the resolution set in Settings.

## Acceptance scenarios

- [SC-0123](../sc/SC-0123.md) Selection converted to a transparent pixel image
- [SC-0124](../sc/SC-0124.md) Whole document conversion and refused conversions

## Implementation and evidence

Implementing in 0.10.0: packages/domain/src/rasterize.ts, the `rasterize` method of
packages/renderer/src/canvas-renderer.ts, `rasterizePng` and `rasterizeSelection` in
apps/web/src/app/editor.service.ts, and the Import flyout and dialog of the shell. The
Object > Rasterize entry awaits the owner's decision.

Automated: packages/domain/tests/rasterize.test.ts (target choice, paint area, pixel grid,
limits, margin frame, in-memory crop and aliasing) and apps/web/tests/rasterize.test.ts (pixels against a golden
image drawn with the canvas API at 150 ppi, placement, group, margin, aliasing, whole document
against PNG export, refusals, the preview entry point, a single readback of the drawing and the shell wiring). Run with
`npx vitest run` on the feat/convert-to-pixel-image branch for 0.10.0; the full suite passed
(143 files, 997 tests). Human review and the paused Sonar analysis are outstanding.
<!-- navigation:generated -->

## Navigation

[Category index](INDEX.md) | [Context entry](../INDEX.md) | [ADR-0024](../adr/ADR-0024.md) | [ADR-0036](../adr/ADR-0036.md) | [FEAT-0007](FEAT-0007.md) | [FEAT-0010](FEAT-0010.md) | [FEAT-0029](FEAT-0029.md) | [PO-0001](../po/PO-0001.md) | [SC-0119](../sc/SC-0119.md) | [SC-0123](../sc/SC-0123.md) | [SC-0124](../sc/SC-0124.md) | [SC-0149](../sc/SC-0149.md) | [SEC-0005](../sec/SEC-0005.md) | [implementation: packages/domain/src/rasterize.ts](../../packages/domain/src/rasterize.ts) | [implementation: packages/renderer/src/canvas-renderer.ts](../../packages/renderer/src/canvas-renderer.ts) | [implementation: apps/web/src/app/editor.service.ts](../../apps/web/src/app/editor.service.ts) | [implementation: apps/web/src/app/app.component.ts](../../apps/web/src/app/app.component.ts) | [tests: packages/domain/tests/rasterize.test.ts](../../packages/domain/tests/rasterize.test.ts) | [tests: apps/web/tests/rasterize.test.ts](../../apps/web/tests/rasterize.test.ts)
