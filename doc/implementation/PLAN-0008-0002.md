---
id: "PLAN-0008-0002"
title: "Appearance, measurement aids and guide layers"
status: "implementing"
domain: "implementation"
owners: ["Yoshikemolo"]
applies_to: ["ximply-design-studio"]
related: ["FEAT-0004", "FEAT-0007", "FEAT-0008", "FEAT-0009", "PLAN-0008-0001", "ADR-0024"]
source: ["Owner appearance, units, ruler, guide and grid requirements"]
---

# Appearance, measurement aids and guide layers

## Problem and acceptance

Expose the same fill/stroke picker from the context bar and overlapping tool-rail
swatches, including no paint, black, white, color and stroke width. Swap fill/stroke
without losing the selected target. Move theme switching beside the locale selector.
Split the context bar into independently visible persisted View > Layout blocks.

Distance and font units must affect displayed dimensional values and editing across
the interface without rescaling document geometry. Use px, pt, mm, cm, in and ft with
96 px per inch and 72 points per inch. These are document units, not a claim about
physical monitor calibration. Rulers and grid respond to canonical sizes, zoom and
scroll. Each of rulers, guides and grid has separate visibility, snapping and radius.

Drag from a ruler to create a horizontal or vertical guide as an editable layer.
Guide dragging moves it; releasing outside the artboard deletes it. Cancel restores
the prior document. Hidden/locked guides do not accidentally intercept drawing.
Layers can be reordered with group identity/order kept consistent. Guides are editing
aids and must never appear in artwork export.

## Scope and contracts

Domain measurement helpers own conversion, bounded tick generation and snapping.
All stored geometry remains pixels. Snap tolerances are CSS pixels divided by zoom.
Preference changes do not resize artwork. The optional native-v2 path layer `guide`
field is either vertical or horizontal; position uses x or y respectively. Guides
cannot be symbol definitions or grouped/symbol-linked artwork. Paint accepts the
explicit literal none or hex8 RGBA in fill/stroke, while the document background remains hex.
Frontend and authenticated API validators must agree on acceptance and rejection.
Renderer skips guides and skips canvas fill/stroke operations for absent paint.
Shared picker and overlay UI stay independent of export rendering. No dependencies,
transport changes or production service changes are introduced. See the proposed
[contract decision](../adr/ADR-0024.md).

## Acceptance oracles

- 25.4 mm and 1 inch both equal 96 px; 72 pt equals 1 inch. Unit changes preserve
  the exact stored coordinates, font sizes and stroke widths until edited.
- Missing/invalid preference values recover predictably; failed saves do not mutate
  the live setting. Ruler/grid generation remains bounded for tiny steps/large zoom.
- Independent snap settings select the nearest eligible coordinate only within the
  configured screen-space radius. Hidden aids do not snap, and no eligible target
  leaves the original point unchanged.
- No-color vector/text pixels stay transparent where neither fill nor stroke paints.
  SVG represents absent paint explicitly. Native/API roundtrips preserve guide and
  color state; malformed fields and incompatible native-v1 data are rejected.
- Guide creation, movement, outside deletion, cancellation and layer reorder have
  coherent undo/redo. Hidden/locked guides and grouped layer order have tests.
- Shared picker actions and all dimensional inputs route to canonical values;
  changing units is display-only. Rulers/guide overlays follow viewport coordinates.

## Sequence and documentation impact

Implement model/API/render contracts and pure measurement/preferences in parallel.
Integrate editor history, guide operations, layer ordering and snapping. Integrate
shared appearance controls, global unit displays, measurement overlays and layout.
Publish tested checkpoints with versioned Markdown notes and explicit branch/SHA.
Update feature traceability, generated navigation and bundled release metadata.

## Failure, migration and rollback

Existing files retain geometry and default to no guide metadata. New none paints and
guide metadata are native-v2 extensions that older readers may reject or fail to
preserve. Migration: retain original files and use this application version for new
saves; reverting application code does not convert files. Exporting SVG/PNG omits
editing guides and is not a substitute for the editable native document.
Pointer cancellation restores the previous document; outside release deletes only
the actively dragged guide. Grid/ruler settings must be finite and positive, and
rendered tick/line counts are bounded. Keep current quality gates unchanged.

## Validation and handoff

Record actual frontend/API/engineering tests, production build and packaged CI at
the checkpoint. Browser visual acceptance, strict Sonar evidence and required human
review remain distinct from feature-branch preview availability.

Independent paint alpha is separate from layer opacity. The shared picker preserves
RGB when editing alpha and preserves alpha when editing RGB. Native/API roundtrips
retain each channel; Canvas and SVG alpha combine with overall layer opacity.

## Contextual action follow-up

Capture target identity and revision for right-click menus on canvas, guides and
layer rows. Arrange complete sibling blocks within their parent; never flatten or
cross group ownership. Node actions preserve opposite handles unless converting
smoothness, and disabled/locked/stale targets reject mutations. Native editing-field
menus remain available. Validation covers group boundaries, stale node indices,
undo, keyboard navigation, dismissal and pointer coordinate routing. No native/API
changes or migration are needed. Revert the component/service patch to roll back.

## Typography and guide-lock follow-up

Use optional native-v2 typography and text frame metadata, strict frontend/API
validation, and shared measured composition for canvas and SVG. Preserve legacy
text without metadata and source strings during automatic EN/ES hyphenation.
Expose family, weight/style, leading, spacing, scales, baseline, alignment,
decoration and frame controls through localized properties and icon actions.

Keep global guide locking in preferences, separate from individual layer locking:
block repositioning existing guides but permit ruler creation. Reuse layer lock
icons immediately before guide visibility controls.

ADR-0025 records the pinned hyphen dependency and remaining provenance review.
Native textarea caret fidelity, advanced OpenType, per-character styles and linked
frames are outside this preview. New metadata requires updated native readers;
retain original files before saving. Revert the feature commit for code rollback.

## Published preview evidence

- c57384d257cd16e4ed462759c0eaad25ff739fd5: document/model/API appearance and measurement foundation.
- b84d79c0e440d7f3d52650758d4afe077e3561d2: 0.3.5-alpha.1 shared palette, measurement aids and guide/layer operations; 151 frontend tests, 39 API tests with 100 subtests, 74 engineering tests and production build passed.
- f8f5c7f1afad0bd5688b090434c8e31a41ca6e18: 0.3.6-alpha.1 contextual actions; 172 frontend tests, 74 engineering tests and production build passed. GitHub editor/container and contribution workflows passed for this exact revision and both preceding checkpoints.

The typography checkpoint uses npm test, npm run build, API pytest, engineering
unittest, context/changelog/knowledge/documentation checks and packaged stylesheet
validation. Automated checks do not replace browser visual acceptance, strict Sonar
evidence or required review for integration.

## Settings category navigation follow-up

Replace the combined scrolling settings form with a two-column category menu and
active form panel. Keep every existing control and its preference binding. Group
cursor, selection, measurements and shortcuts rationally, localize navigation,
provide keyboard access and keep forms usable at narrow widths. Cancel unfinished
shortcut capture on category changes. This is a presentation change with no native
model, API, dependency or preference-storage migration. Reverting the UI commit
restores the old layout without deleting settings. Validate category routing,
shortcut capture cancellation, existing controls and production template compilation.
The persistent owner preference and future memory/storage examples are recorded in
AGENTS.md and the UX direction document, without adding inactive placeholder fields.

## Stroke appearance and style tools follow-up

Add native-v2 optional complete strokeStyle metadata with center/inside/outside
alignment, round/bevel/miter joins and butt/square/round caps. Absence preserves
legacy centered round stroke behavior. Inside/outside applies to closed vector
contours; open paths and live text remain centered. Match canvas/SVG coverage and
alpha through clipping/masks rather than double-compositing strokes. Use the same
miter limit in both renderers. Validate enums and full shape in frontend and API,
including symbol definitions. No runtime dependency is introduced.

The shared stroke picker exposes icon actions with localized explanations. Add
Eyedropper and Paint Bucket tools with fill/stroke/both scopes, including alpha,
width and stroke geometry. Sample semantic object styles rather than image pixels;
apply mutations through history, respecting layer/group locks and excluding guides.
Preserve configured shortcuts when introducing new tool bindings, resolving any
existing user collision by leaving the new action unassigned.

Acceptance covers actual stroke pixel bounds for closed shapes, open cap extents,
joins, holes, SVG consistency, native/API round trips, tool scope, locks, undo and
new drawing defaults. Retain original documents before saving added metadata;
older readers may reject it. Revert the feature commit for code rollback; this does
not downgrade newly saved files. Bucket changes object styles; region flood filling
and live-paint topology are outside this implementation.

## Editable object blends follow-up

Create an editable blend between two selected sibling vector objects or equally
sized vector groups, ordered by document stacking. Retain original endpoint IDs
and emit actual intermediate path layers so existing rendering and SVG/PNG export
use the same artwork. Add bounded intermediate steps and linear/ease-in/ease-out/
ease-in-out distribution. Interpolate path anchors and control handles, geometry,
paint alpha/colors, width and opacity. Normalize differing node counts through
bounded exact cubic subdivision; reject incompatible contour closure/counts,
unsupported content and document capacity before mutation.

Native-v2 optional blend definitions store endpoint and generated-layer references,
step count, easing and group identity. Validate reference uniqueness, group/order
ownership, limits and supported endpoint types in frontend/API. Derived geometry
regenerates from endpoint edits, preserving stable step IDs. Native parse can
normalize derived caches after validation. No new dependency is needed.

Expose Make/Expand/Release actions and count/easing controls with localized icon
explanations. Allow direct endpoint object/group selection for editing. Expanding
keeps all objects as ordinary groups; releasing removes generated steps and keeps
endpoints. Ungrouping expands first so intermediate edits are never overwritten;
normal grouping can reorganize expanded objects independently. Preserve undo across
creation, settings changes, endpoint edits, expansion and release. Guard locked
members and reconcile references after deletion, duplication and grouping.

Acceptance includes exact midpoint/style oracles, easing distribution, unequal
vertex counts, independent groups, native/API round trips, export equivalence,
undo/redo and layer/depth limits. Migration: retain original files before saving
blend metadata, since older readers may reject it. Revert code for rollback; saved
files are not automatically downgraded. Spine editing, orientation along a path,
text/image blending and arbitrary incompatible contour correspondence are excluded
from this bounded preview; controls must report unsupported selections explicitly.
