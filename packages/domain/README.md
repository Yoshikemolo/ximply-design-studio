# domain

Pure TypeScript native-document validation, geometry, selection, transforms, history and SVG export. It imports no Angular or Three.js. Tests provide independent geometry, boundary and escaping oracles.
