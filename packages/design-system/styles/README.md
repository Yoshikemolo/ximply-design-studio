# Central style entry point

Status: Planned. Keep all authored SCSS here. Implement tokens, semantic light/dark
themes, layout primitives, component styles and accessibility styles. Theme plugins
provide validated tokens and assets; no arbitrary injected stylesheet. Tests verify
contrast, focus and consistency across shell and plugin panels.
